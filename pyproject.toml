[project]
name = "celiumsnn"
version = "0.1.0"
description = "SNN model architecture derived from the CeliumNeUR constraint set (Mycelium)"
requires-python = ">=3.10"
license = "Apache-2.0"

[tool.pytest.ini_options]
pythonpath = [".", "celiumneur/golden"]
testpaths = ["tests"]
